---
// Hero section for homepage
---

<section class="hero">
    <div class="container">
        <h1 class="hero-title">
            I help Law Firms and Legal Agencies run successful ad campaigns
        </h1>
        <p class="hero-subtitle">
            <strong
                >Get higher quality leads, sign more cases, and take your firm
                to new heights</strong
            >
        </p>
        <div class="hero-ctas">
            <a href="/schedule" class="btn btn-primary">
                Schedule your FREE strategy session
            </a>
            <a href="/reviews" class="btn btn-secondary">
                What people say about my work
            </a>
        </div>
        <picture>
            <source
                media="(max-width: 768px)"
                srcset="/Website-Logos-387x80.png"
            />
            <img
                src="/Client-Logo-Banners%20(768%20x%2094%20px).png"
                alt="Client logo banners"
                class="hero-logos"
                loading="lazy"
                decoding="async"
            />
        </picture>
    </div>
</section>

<style>
    .hero {
        padding: var(--space-12) 0;
        text-align: center;
    }

    .hero-title {
        font-size: 3rem;
        font-family: var(--font-display);
        margin-bottom: var(--space-4);
        line-height: 1.2;
    }

    .hero-subtitle {
        font-size: 1.375rem;
        color: var(--muted-foreground);
        margin-bottom: var(--space-8);
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
    }

    .hero-ctas {
        display: flex;
        gap: var(--space-4);
        justify-content: center;
        align-items: center;
        margin-bottom: var(--space-8);
    }

    .hero-logos {
        display: block;
        margin: 0 auto;
        max-width: 100%;
        height: auto;
    }

    /* Mobile responsive */
    @media (max-width: 768px) {
        .hero {
            padding: var(--space-8) 0;
        }

        .hero-title {
            font-size: 2rem;
        }

        .hero-subtitle {
            font-size: 1.125rem;
        }

        .hero-ctas {
            flex-direction: column;
            width: 100%;
        }

        .hero-ctas .btn {
            width: 100%;
        }

        .hero-logos {
            width: 386px;
            max-width: 100%;
        }
    }
</style>
